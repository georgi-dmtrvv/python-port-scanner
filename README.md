# Python Port Scanner

A multithreaded TCP port scanner written in Python.

## Features
- ThreadPoolExecutor for parallel scanning
- Port range chunking for performance
- TCP connection testing using sockets

## Usage
Run the script:
